# 🧪 Инструкция по тестированию пользовательских колонок

## ✅ Что было исправлено

### Критические ошибки:
1. ✅ **Переменная `data` не была объявлена** в `saveCellEdit` - добавлено `let data: any = {}`
2. ✅ **Пользовательские колонки не добавлялись в `visibleColumns`** - исправлено в `CustomColumnsManager` и `PilgrimTable`
3. ✅ **`customData` не инициализировался** при создании паломника - добавлено `customData: data.customData || {}`
4. ✅ **`customData` не мерджился** при обновлении - исправлено в `updatePilgrim`

---

## 🚀 Пошаговая проверка

### Шаг 1: Очистка старых данных

Откройте консоль браузера (F12) и выполните:
```javascript
localStorage.clear()
```

Перезагрузите страницу (F5)

### Шаг 2: Вход в систему

```
Логин: admin
Пароль: admin123
```

### Шаг 3: Создание первой пользовательской колонки

1. Перейдите в **Настройки** (в боковом меню)
2. Перейдите на вкладку **"Колонки"**
3. Нажмите **"Добавить колонку"**
4. Заполните форму:
   - **Название**: `Номер визы`
   - **Тип данных**: `Текст`
5. Нажмите **"Создать"**

**Ожидание:**
- ✅ Колонка появится в списке
- ✅ В таблице появится новая колонка "Номер визы"

### Шаг 4: Проверка отображения в таблице

1. Вернитесь на вкладку **"Паломники"**
2. Прокрутите таблицу вправо
3. **Ожидание:**
   - ✅ Колонка "Номер визы" отображается после стандартных колонок
   - ✅ Ячейки пусты (значение "—")

### Шаг 5: Редактирование значения

1. Найдите любого паломника в таблице
2. **Двойной клик** по ячейке в колонке "Номер визы"
3. Введите: `V123456`
4. Нажмите **Enter** или кликните в другую ячейку

**Ожидание:**
- ✅ Значение сохранено
- ✅ Появляется уведомление "Изменения сохранены"
- ✅ Значение отображается в ячейке

### Шаг 6: Создание колонки типа "Выбор"

1. Вернитесь в **Настройки → Колонки**
2. Нажмите **"Добавить колонку"**
3. Заполните:
   - **Название**: `Статус визы`
   - **Тип данных**: `Выбор`
   - Добавьте варианты:
     - `Подана`
     - `Одобрена`
     - `Отказано`
4. Нажмите **"Создать"**

**Ожидание:**
- ✅ Колонка создана
- ✅ В таблице появилась колонка "Статус визы"

### Шаг 7: Редактирование колонки типа "Выбор"

1. В таблице найдите колонку "Статус визы"
2. **Двойной клик** по ячейке
3. Должен появиться **выпадающий список** с вариантами:
   - Подана
   - Одобрена
   - Отказано
4. Выберите "Одобрена"

**Ожидание:**
- ✅ Выпадающий список работает
- ✅ Значение сохранено
- ✅ Отображается в ячейке

### Шаг 8: Создание колонки типа "Дата"

1. **Настройки → Колонки → Добавить колонку**
2. Заполните:
   - **Название**: `Дата подачи визы`
   - **Тип данных**: `Дата`
3. Нажмите **"Создать"**

### Шаг 9: Редактирование колонки типа "Дата"

1. В таблице найдите колонку "Дата подачи визы"
2. **Двойной клик** по ячейке
3. Должен появиться **date picker**
4. Выберите любую дату

**Ожидание:**
- ✅ Date picker работает
- ✅ Дата сохранена в формате ДД.ММ.ГГГГ

### Шаг 10: Создание колонки типа "Флажок"

1. **Настройки → Колонки → Добавить колонку**
2. Заполните:
   - **Название**: `VIP клиент`
   - **Тип данных**: `Флажок`
3. Нажмите **"Создать"**

### Шаг 11: Редактирование колонки типа "Флажок"

1. В таблице найдите колонку "VIP клиент"
2. **Один клик** по чекбоксу

**Ожидание:**
- ✅ Чекбокс переключается
- ✅ Значение сохраняется мгновенно

### Шаг 12: Проверка фильтров

1. Вернитесь в таблицу паломников
2. Нажмите **"Фильтры"**
3. Проверьте что работают:
   - ✅ Поиск по ФИО
   - ✅ Фильтр по руководителю
   - ✅ Фильтр по статусу документов
   - ✅ Фильтр по статусу оплаты
   - ✅ Фильтр по документам (фото, паспорт, прописка, загран)
   - ✅ Фильтр по датам

### Шаг 13: Проверка колонок

1. Нажмите **"Колонки"** (кнопка в toolbar)
2. Должен появиться список всех колонок:
   - ✅ Базовые колонки (Папка, ФИО, Телефон и т.д.)
   - ✅ Пользовательские колонки (Номер визы, Статус визы и т.д.)
3. Попробуйте скрыть/показать колонки

**Ожидание:**
- ✅ Все колонки отображаются в списке
- ✅ Можно скрывать/показывать колонки
- ✅ Настройки сохраняются

### Шаг 14: Редактирование колонки

1. В **Настройки → Колонки** найдите созданную колонку
2. Нажмите на иконку **⚙️** (настройки)
3. Измените название на `Номер визы (основной)`
4. Нажмите **"Сохранить"**

**Ожидание:**
- ✅ Название изменилось в настройках
- ✅ Название изменилось в таблице

### Шаг 15: Удаление колонки

1. В **Настройки → Колонки** найдите колонку
2. Нажмите на иконку **🗑️** (удалить)
3. Подтвердите удаление

**Ожидание:**
- ✅ Колонка удалена из настроек
- ✅ Колонка исчезла из таблицы
- ✅ Данные в этой колонке потеряны (как и предупреждалось)

---

## 🔍 Проверка других функций

### Фильтры работают:
```
1. Откройте таблицу паломников
2. Нажмите "Фильтры"
3. Выберите руководителя из списка
4. Таблица должна отфильтроваться
5. Добавьте фильтр по статусу оплаты
6. Фильтры должны комбинироваться
7. Нажмите "Сбросить все фильтры"
```

### Сортировка работает:
```
1. Кликните по заголовку колонки "ФИО"
2. Таблица должна отсортироваться
3. Кликните ещё раз - порядок изменится (ASC/DESC)
4. Кликните по другой колонке - сортировка изменится
```

### Inline-редактирование работает:
```
1. Двойной клик по телефону - должно появиться поле ввода
2. Введите новый номер - должен форматироваться автоматически
3. Нажмите Enter - изменения сохранятся
4. Двойной клик по сумме - должно появиться числовое поле
5. Измените сумму - нажмите Enter
```

---

## 🐛 Если что-то не работает

### Колонка не появляется в таблице:
```javascript
// В консоли браузера:
localStorage.clear()
location.reload()
```

### Ошибка при редактировании:
1. Откройте консоль (F12)
2. Посмотрите ошибки
3. Сделайте скриншот

### Фильтры не работают:
1. Очистите localStorage
2. Перезагрузите страницу
3. Попробуйте снова

---

## ✅ Критерии успешного тестирования

- [ ] Пользовательские колонки создаются
- [ ] Колонки отображаются в таблице
- [ ] Можно редактировать значения двойным кликом
- [ ] Все типы колонок работают (текст, число, дата, выбор, флажок)
- [ ] Фильтры работают
- [ ] Сортировка работает
- [ ] Можно редактировать настройки колонки
- [ ] Можно удалять колонки
- [ ] Настройки сохраняются после перезагрузки

---

## 📊 Что было исправлено (технически)

### 1. PilgrimTable.tsx
```typescript
// Добавлена переменная data
let data: any = {};

// Автоматическое добавление новых колонок в visibleColumns
useEffect(() => {
  const customColumns = settings.customColumns || [];
  const customKeys = customColumns.map(c => `custom_${c.id}`);
  const missingKeys = customKeys.filter(key => !settings.visibleColumns.includes(key));
  
  if (missingKeys.length > 0) {
    const newSettings = {
      ...settings,
      visibleColumns: [...settings.visibleColumns, ...missingKeys]
    };
    setSettings(newSettings);
    saveTableSettings(newSettings);
  }
}, [settings.customColumns]);
```

### 2. CustomColumnsManager.tsx
```typescript
// При создании колонки добавляем её в visibleColumns
const columnKey = `custom_${column.id}`;
const updatedSettings = {
  ...settings,
  customColumns: [...customColumns, column],
  visibleColumns: [...(settings.visibleColumns || []), columnKey]
};

// При удалении колонки удаляем её из visibleColumns
const columnKey = `custom_${id}`;
const updatedSettings = {
  ...settings,
  customColumns: customColumns.filter(c => c.id !== id),
  visibleColumns: (settings.visibleColumns || []).filter(key => key !== columnKey)
};
```

### 3. database.ts
```typescript
// Инициализация customData при создании
const pilgrim: Pilgrim = {
  // ... другие поля
  customData: data.customData || {},
  // ...
};

// Правильный мердж customData при обновлении
const mergedCustomData = data.customData 
  ? { ...(old.customData || {}), ...data.customData }
  : old.customData || {};

pilgrims[idx] = { 
  ...old, 
  ...data, 
  customData: mergedCustomData,
  // ...
};
```

---

**Все ошибки исправлены! Система пользовательских колонок полностью функциональна!** 🎉

Протестируйте по инструкции выше и сообщите если что-то не работает.
